apiVersion: v1
kind: Pod
metadata:
  name: busybox
  namespace: todo-app 
spec:
  containers:
  - image: yauritux/busybox-curl
    command:
      - sleep
      - "3600"
    imagePullPolicy: IfNotPresent
    name: busybox
  restartPolicy: Always
