# Create wikitodo cronjob

Creates cronjob to automatically add a new random wikipedia article as a todo in the todo-app

## Usage

```console
$ kubectl apply -f randomwikitodo-cronjob.yaml
```

By default set to run every minute
