apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rsg-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service: 
            name: random-string-generator-svc
            port: 
              number: 2345
